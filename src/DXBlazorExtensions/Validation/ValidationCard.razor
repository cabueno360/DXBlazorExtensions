


@if (!IsValid)
{
    <div role="alert" class="alert alert-warning" aria-live="assertive" aria-atomic="true" aria-relevant="text">
        @foreach (string message in @Messages)
        {
            <p>@message</p>
        }
    </div>
}
